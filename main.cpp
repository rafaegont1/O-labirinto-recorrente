#include <iostream>

typedef struct boy {
  unsigned short x, y, lives, items;
  boy() : x(0), y(0), lives(10), items(0) {}
} Boy;

void joystick(unsigned short *restrict mat, unsigned short *restrict i, unsigned short *restrict j) {
}

int main(int argc, char *argv[]) {
  struct boy;
  unsigned short *mat, mat_sz;

  while(life != 0) {
  }

  return 0;
}
